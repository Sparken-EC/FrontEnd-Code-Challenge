// import { get_data } from '@/api/get_data';

describe('Get Rick & Morty data suite', () => {
	beforeEach(async () => {
		// data = await get_data();
	});

	it('Should return character list', () => {
		expect(true).toBe(true);
	});
});
